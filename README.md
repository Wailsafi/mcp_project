to get started with this project:
clone the repo 
install uv using pip install uv it you are  on windows  otherwise  use pip3 install uv it you are on linux or macos 
 add the bin to the enivronement variables so you can execute uv conmmands 

 run uv sync so your .env environement gets created with the needed requirements.
